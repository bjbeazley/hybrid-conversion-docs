services:
  material-server:
    image: app:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/docs
    ports:
      - 8000:8000
    develop:
      watch:
        - action: restart
          path: docs
